services:
  - type: web
    name: family-schedule
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: FAMILY_PASSWORD
        sync: false
